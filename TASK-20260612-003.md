┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 批复工单                      │
│                                                         │
│  编号: TASK-20260612-003                                │
│  主题: 审计 P0 修复——竞态/泄漏/吞异常/像素/并发            │
│  指派: opencode                                         │
│  基线: c65ce53                                           │
│  优先级: 高                                              │
│  改动量: 2 文件 actuator.py + verifier.py + cache.py    │
└─────────────────────────────────────────────────────────┘

## 审计来源

桌面文件 `审计1.txt`，弥亚已批复：全部通过。

## 需求

修复 5 个阻塞级 bug：

1. **竞态：_precheck 移至信号采集之前** — actuator.py 所有 verify 函数中，`capture_signals` 必须在 `_precheck` 之后
2. **资源泄漏：Image/BytesIO close** — verifier.py 所有 PIL Image 和 io.BytesIO 用 `with` 或 `try/finally` 确保释放
3. **静默吞异常→假阳性** — compare_signals 增加 `errors` 字段，明确报告哪些信号采集失败（foreground/screenshot/focus），而非填 None 让 LLM 猜
4. **像素 diff：尺寸变化返回 size_changed 而非假 100%**，region 边界 `+1`（right = max_x+1, bottom = max_y+1）
5. **并发安全：ControlCache 加 threading.Lock()** — 保护 _next_id、_id_to_rt、_rt_to_id

## 约束

│  M1 · _precheck 必须置于 capture_signals 之前，不能再有例外
│  M2 · close/min 操作不要求 pixel verify（审计 #5 已另开工单处理），本单只修其他动作
│  M3 · errors 字段返回 list[dict]，而非字符串，保持结构化
│  M4 · Lock 粒度：每个读写方法内部加锁，不要让外部调用方管理锁

## 改动文件

│ 文件                  │ 操作       │ 行数  │
│───────────────────────│────────────│───────│
│ engine/actuator.py    │ 改         │ ~30   │
│ engine/verifier.py    │ 改         │ ~40   │
│ engine/cache.py       │ 改         │ ~15   │

## 不改动

- auditor.py — 本单不涉及 auditor 重构
- tools/ 目录 — 修复集中在 engine 层
- window_action close/min 验证 — 移入 TASK-004

─────────────────────────────────────────────────────────
弥亚 · 2026-06-12 13:49
