┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 工单                        │
│                                                         │
│  编号: TASK-20260612-002                                │
│  主题: 操作后多维信号自验证框架                           │
│  指派: opencode                                         │
│  基线: c25f139                                          │
│  优先级: 高                                              │
│  改动量: 3 文件 约 210 行                                 │
└─────────────────────────────────────────────────────────┘

## 需求

当前每个 deskhand 工具（click/type/press/scroll/select/drag/window）只返回 `{"success":true}`，
LLM 无法判断操作是否真的产生了预期效果——这是"盲操作"。

需要每个操作执行后自动采集多维信号，计算前后差异，返回结构化验证数据给 LLM 做判断。

## 约束

│ M1 · 零新增 pip 依赖。所有信号采集用已依赖的 win32gui/win32api/uiautomation/PIL
│ M2 · 不影响旧行为。引入 `verify=True` 参数默认开启，但 `verify=False` 时回退到纯操作
│ M3 · 采集失败不影响操作返回。如截图采集失败，visual_diff 写 null 而非炸掉整个操作
│ M4 · 不改动 tools/*.py 的接口签名。LLM 侧参数不变，只是在返回值里多出 `_verify` 字段
│ M5 · 每个操作类型只采集相关的信号子集，不要全量采（浪费性能）

## 改动方案

### 3.1 新建 `engine/verifier.py`（~150行）

三个核心函数：

**`capture_signals(target_rect=None)` → dict**

采集以下零成本信号：

| 信号 | 采集方式 | 字段 |
|------|---------|------|
| 前台窗口句柄 | `GetForegroundWindow()` | `foreground_hwnd` |
| 前台窗口标题 | `GetWindowText(hwnd)` | `foreground_title` |
| 光标位置 | `GetCursorPos()` | `cursor_x, cursor_y` |
| 控件树快照（UIA） | `scan_active_window()` 轻量扫描 | `uia_root_id` + 关键控件列表 |
| 目标区域截图 | PIL `ImageGrab.grab(bbox)` | `screenshot_bytes`（仅在需要时传 target_rect） |
| 时间戳 | `time.perf_counter()` | `ts` |

实现要点：
- 目标区域截图只在 `target_rect` 指定时才截，减少不必要开销
- UIA 控件树快照只采集顶层+直接子节点（depth=2），不需要完整递归
- 所有异常静默捕获，返回 `null` 而非崩溃

**`compare_signals(before, after)` → dict**

计算两组信号的差异：

| 对比维度 | 输出字段 | 计算方式 |
|---------|---------|---------|
| 前台窗口变化 | `foreground_changed` | hwnd 不同 |
| 窗口标题变化 | `title_changed`, `title_before`, `title_after` | 字符串对比 |
| 光标移动量 | `cursor_moved`, `cursor_delta_x`, `cursor_delta_y` | 坐标差 |
| 像素变化 | `visual_changed`, `visual_diff_percent`, `visual_diff_region` | PIL 像素级对比（阈值 5 灰度值） |
| 控件焦点变化 | `focus_changed`, `focus_before`, `focus_after` | UIA HasKeyboardFocus |

**像素对比算法**（关键）：
```python
def pixel_diff(img_before: Image, img_after: Image, threshold: int = 5) -> dict:
    # 两张图逐像素对比，差异超过 threshold 算"变化像素"
    # 返回: {changed, percent, bbox_of_changed_area}
    # bbox 是变化区域的最小外接矩形
```

### 3.2 修改 `engine/actuator.py`（~60行改动）

为 7 个动作函数嵌入验证包装。以 `click` 为例：

```python
def click(cid: int, button: str = "left", double: bool = False,
          hover: bool = False, verify: bool = True, cache=None) -> dict:

    if verify:
        # 1. 操作前采集（含目标控件区域截图）
        control = _get_control_by_id(cid, cache)
        target_rect = control.BoundingRectangle  # 获取目标区域
        before = capture_signals(target_rect=...)

    # 2. 执行原操作
    result = _do_click(control, button, double, hover)  # 原逻辑

    if verify:
        # 3. 等待 200ms
        time.sleep(0.2)
        # 4. 操作后采集
        after = capture_signals(target_rect=...)
        # 5. 对比
        result["_verify"] = compare_signals(before, after)

    return result
```

**每个动作类型的信号子集**（按 M5 约束）：

| 动作 | 采集的信号 |
|------|-----------|
| click/hover | foreground, cursor, title, visual(目标控件区域), uia焦点 |
| type_text | foreground, title, visual(目标控件区域), uia控件值 |
| press | foreground, title, visual(活跃窗口区域) |
| scroll | foreground, visual(目标控件区域), uia滚动位置 |
| select_text | foreground, visual(目标控件区域) |
| drag | foreground, cursor, visual(始末区域) |
| window_action | foreground, title, visual(窗口区域) |

### 3.3 最终 LLM 看到的返回值示例

```json
{
  "success": true,
  "action": "click",
  "id": 6,
  "_verify": {
    "foreground_changed": true,
    "foreground_hwnd_before": "0x00123ABC",
    "foreground_hwnd_after": "0x00456DEF",
    "title_changed": false,
    "cursor_moved": true,
    "cursor_delta_x": 421,
    "cursor_delta_y": -115,
    "visual_changed": true,
    "visual_diff_percent": 3.4,
    "visual_diff_region": {"left": 1048, "top": 282, "right": 2299, "bottom": 1175},
    "focus_changed": false
  }
}
```

LLM 从此可以推理：
- `foreground_changed:true` → QQ 窗口确实被激活了
- `visual_diff_percent:3.4` → 画面有微小变化（正常，激活窗口可能有边框高亮）
- `cursor_moved:true` → 鼠标确实移到了目标控件

## 不改动

- tools/*.py — 不改接口签名，只是 actuator 返回值自动多了 `_verify`
- main.py — 不涉及
- cache.py — 不涉及
- scanner.py — 不涉及（verifier 自己轻量扫描）
- _conf_schema.json — 不涉及
- metadata.yaml — 不涉及

─────────────────────────────────────────────────────────
弥亚 · 2026-06-12 05:16
