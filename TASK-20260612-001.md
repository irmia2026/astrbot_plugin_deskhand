┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 工单                        
│                                                         
│  编号: TASK-20260612-001                                
│  主题: AstrBot 插件 astrbot_plugin_deskhand — 精准 Windows GUI 操控能力 
│  指派: opencode                                         
│  基线: N/A（新项目）                                     
│  优先级: 高                                              
│  改动量: 新建 16 文件，预计 ~1150 行                       
│                                                         
│  📂 参考源码                                            
│  AstrBot 主仓库: D:\Irmia\temp\astrbot-fork              
│  │  ├── samples/         ← 插件官方示例                 
│  │  ├── astrbot/         ← AstrBot 核心代码             
│  │  └── dashboard/       ← Web 管理面板                 
│  Devkit 插件（完整参考）: D:\opencode\irmia_devkit_open               
│  │  ├── main.py              ← 工具注册 & Tool 写法范例              
│  │  ├── metadata.yaml        ← 插件元信息格式                       
│  │  ├── _conf_schema.json    ← 配置 Schema 格式                     
│  │  ├── requirements.txt     ← 依赖声明格式                         
│  │  ├── ARCHITECTURE.md      ← 项目哲学 & 目录规范                  
│  │  └── tools/               ← 各工具模块参考                       
└─────────────────────────────────────────────────────────┘

## 需求

开发一个独立的 AstrBot 插件 `astrbot_plugin_deskhand`，让 LLM Agent 能通过自然语言精准操控 Windows 桌面 GUI —— 不是截图+键鼠模拟，而是走 MS UI Automation (UIA) 控件级交互。

核心用例：
```
用户: "把记事本第三行删掉，然后保存到桌面"
  → Agent 调用 desk_state() 拿到控件树
  → 看到 Edit[id=1] 的 Value="hello\nworld\ntest"
  → desk_type(id=1, line=3, text="") 删除第三行
  → desk_click(id=3) 点击"保存"按钮
```

## 约束

│  M1 · 不依赖视觉模型（vision optional，不做硬要求）
│  M2 · 底层用 `uiautomation` 库（纯 Python MS UIA COM 封装），不用 pywinauto（太重）
│  M3 · 独立插件，不放 devkit 工具箱
│  M4 · 控件 id 必须在多次 state() 调用之间保持稳定（用 UIA RuntimeId 做映射）
│  M5 · state() 输出控制在一屏内（≤2000 token），只采集活跃窗口及其子树
│  M6 · 所有 UIA 调用必须有超时保护（UIA 碰到卡死的应用会阻塞）
│  M7 · click/type 操作前做 precheck：目标控件仍存在且 enabled
│  M8 · 插件格式符合 AstrBot 发布规范（metadata.yaml / _conf_schema.json / logo.png / requirements.txt）
│  M9 · 必须用 uiautomation 的 UIA 后端（自动初始化 `uiautomation.uia` 模式）。Chromium 内核（WebView2/Electron/Edge）通过 UIA 将网页 DOM 完整暴露为控件树——button→ButtonControl, input→EditControl, link→HyperlinkControl——UIA 后端可直接穿透壳层读取网页内部所有交互元素，不需要 CDP/JS 注入等额外手段

## 架构

```
astrbot_plugin_deskhand/
├── metadata.yaml
├── main.py                 # Star 插件入口，注册 9 个 LLM Tool
├── requirements.txt        # uiautomation, pillow
├── _conf_schema.json
├── logo.png
├── engine/
│   ├── __init__.py
│   ├── scanner.py          # ① 控件树采集 → 结构化 dict
│   ├── actuator.py         # ② click / type / press / scroll 执行器
│   ├── annotator.py        # ③ 截图 + 控件标注（可选视觉反馈）
│   └── cache.py            # ④ id ↔ RuntimeId 映射，LRU 128 条
└── tools/
    ├── __init__.py
    ├── desk_state.py         # Tool: 采集活跃窗口控件树
    ├── desk_click.py         # Tool: 点击（左/右/中/双击）
    ├── desk_type.py          # Tool: 输入文本 / 清空指定行
    ├── desk_press.py         # Tool: 快捷键组合
    ├── desk_drag.py          # Tool: 拖拽（id→id 或 id→坐标）
    ├── desk_scroll.py        # Tool: 对指定控件滚轮
    ├── desk_select.py        # Tool: 选中文本范围
    ├── desk_window.py        # Tool: 窗口管理（最小化/最大化/恢复/关闭/置顶/移动/调整大小）
    └── desk_screenshot.py    # Tool: 截图（可选标注控件框）
```

## 四个引擎模块详解

### scanner.py — 控件树采集（双后端自适应）
- 默认 `backend="uia"` 模式，穿透 WebView2/Electron/Tauri 壳层
- 用 `uiautomation.GetRootControl()` 出发，只采集当前活跃窗口
- 递归遍历控件子树，每个控件输出: `id / role / name / value[:100] / rect / enabled / children`
- **WebView2/Electron 穿透**：遇到 `ClassName` 为 `Chrome_RenderWidgetHostHWND` 的控件时不截断，强制深入递归其子树，即可拿到网页内部的 button/input/link/combobox 等全部交互元素
- **DOM 语义映射**（Chromium UIA 自动完成）：`<button>` → ButtonControl，`<input>` → EditControl，`<a>` → HyperlinkControl，`<select>` → ComboBoxControl
- 普通原生控件（Win32/WPF/Qt）同样以 uia 后端采集，向下兼容
- 深度限制 max_depth=15（WebView2 路径需更深），子节点数限制 max_children=120
- 超时保护：单窗口扫描 ≤5s（WebView2 子树更深），超时截断
- 返回结构化 dict → `desk_state` Tool 用 `json.dumps` 喂给 LLM

### actuator.py — 执行器
- `click(id, button="left", double=False, hover=False)`: cache 查 RuntimeId → `ControlFromHandle` → `GetClickablePoint` → `win32api.SetCursorPos` + `mouse_event`。`hover=True` 时只移动鼠标不点击——用于展开工具提示、悬停子菜单
- `drag(from_id, to_id=None, to_x=None, to_y=None)`: 起点控件 → mouse_down → 移动到终点控件/坐标 → mouse_up
- `type(id, text)`: 查控件 → 优先 `SetValue`（Edit 控件），fallback `SendKeys`（转义 `{}` `\n` `\t`）
- `type(id, line=N, text=…)`: 先 GetValue → split("\n") → 改第 N 行 → SetValue
- `press(keys, action="press")`: `action="press"` 按下即释放；`action="key_down"` 按住不放（配合后续操作如 Shift+逐个点击多选）；`action="key_up"` 释放之前按住的键
- `select_text(id, start, end)`: 查 TextPattern → `GetSelection` → `AddToSelection` 扩展选区（或 fallback: 鼠标移动到 start 坐标 → mouse_down → 拖到 end → mouse_up），选中 id 控件内第 start 到第 end 个字符
- `scroll(id, direction, amount)`: 查 ScrollPattern，上下/左右滚动；若控件不支持 ScrollPattern → fallback 鼠标滚轮 `mouse_event(MOUSEEVENTF_WHEEL)` 在控件中心
- `window(action, hwnd=None, x=None, y=None, w=None, h=None)`: min/max/restore/close/set_topmost/focus/move(x,y)/resize(w,h)。默认操作当前活跃窗口
- 每个操作前 precheck: 控件 handle 仍有效、仍 enabled

### cache.py — ID 稳定性
- UIA 控件有 `RuntimeId`（int tuple），在同一进程生命周期内不变
- 首次扫描时分配自增 id，cache 存 `{id: RuntimeId}`
- 后续 state() 中同一 RuntimeId 复用旧 id
- LRU 淘汰，上限 128 条

### annotator.py — 视觉反馈（可选）
- `screenshot()`: PIL ImageGrab 截屏（指定窗口或全屏）
- `annotate(img, tree_state)`: 在截图上画控件边框 + id 标签
- 供 vision LLM 双重确认

## 九个 LLM Tool

| Tool | 参数 | 返回 |
|------|------|------|
| `desk_state` | （无） | 活跃窗口控件树 JSON，含 id/role/name/value/rect |
| `desk_click` | `id: int, button: str?, double: bool?, hover: bool?` | 点击/悬停结果，含控件 name 和坐标 |
| `desk_type` | `id: int, text: str, line: int?` | 输入结果，含处理行数 |
| `desk_press` | `keys: list[str], action: str?` | 按键结果（action=press/key_down/key_up） |
| `desk_drag` | `from_id: int, to_id: int? to_x: int? to_y: int?` | 拖拽结果（起点→终点） |
| `desk_scroll` | `id: int, direction: str, amount: int?` | 滚动结果（direction=up/down/left/right） |
| `desk_select` | `id: int, start: int, end: int` | 选中结果，含选中文本和范围 |
| `desk_window` | `action: str, hwnd: int? x: int? y: int? w: int? h: int?` | 窗口操作结果（min/max/restore/close/focus/move/resize） |
| `desk_screenshot` | `annotate: bool?` | base64 PNG 截图 |

## Electron / Tauri / WebView2 穿透方案

### 原理

Chromium 内核将网页 DOM 通过 UIA 完整暴露。屏幕阅读器（Narrator/NVDA）正是通过这一机制读取浏览器/Electron 内部网页内容。DeskHand 直接复用同一 UIA 通道，无需任何额外注入或协议。

```
┌─────────────────────────────────┐
│ 原生壳 (Win32 hwnd)             │  ← UIA 能摸到（窗口标题、菜单栏）
│  ┌───────────────────────────┐  │
│  │ WebView2 / Chromium 内核  │  │  ← UIA 深入穿透
│  │  ┌─────────────────────┐  │  │
│  │  │ <button> 发送 </button>│  │  │  ← ButtonControl [id=42]
│  │  │ <input placeholder=…> │  │  │  ← EditControl [id=43]
│  │  │ <a href=…> 链接 </a>  │  │  │  ← HyperlinkControl [id=44]
│  │  └─────────────────────┘  │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### 关键实现

```python
def _is_chromium_host(control):
    """检测是否为 Chromium 渲染宿主"""
    return control.ClassName in (
        "Chrome_RenderWidgetHostHWND",  # Chromium/WebView2
        "Chrome_WidgetWin_0",           # 旧版 Chromium
    )

def scan_control(control, depth=0):
    node = {"id": next_id(), "role": control.ControlTypeName, ...}
    # 普通 Win32 控件可能 GetChildren() 返回空，
    # 但 Chromium 宿主通过 UIA 暴露了完整网页子树
    for child in control.GetChildren():
        node["children"].append(scan_control(child, depth + 1))
    return node
```

### 覆盖范围

| 应用类型 | 之前（win32 后端） | 现在（uia 后端） |
|---------|:---:|:---:|
| **Electron** (QQ, Chatbox, VS Code, GitHub Desktop, Docker Desktop) | 只壳 | ✅ 壳+网页内容 |
| **Tauri** (Clash Verge, AstrBot Desktop) | 只壳 | ✅ 壳+网页内容 |
| **WebView2 独立应用** | 只壳 | ✅ 壳+网页内容 |
| **Edge/Chrome 浏览器** | 地址栏+标签 | ✅ 网页内所有交互元素 |
| **原生 Win32/WPF** | 完整 | ✅ 不变 |
| **Qt 5+** | 完整 | ✅ 不变 |

## 不改动

- 不集成视觉模型 — 插件不做截图理解，截图只做反馈
- 不改变 devkit 工具箱 — 独立发布
- 不操作多桌面 — v1 只操作当前桌面
- 不做 CDP / JS 注入 — Chromium UIA 暴露已经够用

─────────────────────────────────────────────────────────
弥亚  ·  2026-06-12 04:01
