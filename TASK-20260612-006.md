┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 工单                          │
│                                                         │
│  编号: TASK-20260612-006                                │
│  主题: 设计层优化——交互模式/坐标模式/验证分级/工具瘦身      │
│  指派: opencode                                         │
│  基线: c65ce53                                           │
│  优先级: 中                                              │
│  改动量: 3 文件 main.py + scanner.py + actuator.py       │
└─────────────────────────────────────────────────────────┘

## 需求

在保持向后兼容的前提下，增加四项 token 优化功能：

1. **desk_state 交互控件模式** — `desk_state(mode="interactive")` 只返回 Button/Edit/ListItem/MenuItem/CheckBox/ComboBox/TabItem/TreeItem 等可交互控件，过滤 PaneControl/GroupControl/TitleBarControl 等纯结构节点。默认 mode=None 保持全量行为
2. **desk_state 坐标模式** — `desk_state(mode="coords")` 只返回窗口的 name+rect 列表，不递归子树。供 move/resize/排列类操作使用。与 target 参数可组合：`desk_state(target="QQ", mode="coords")`
3. **验证分级** — `desk_click(verify="light")` 只检查 foreground 变化，跳过 pixel diff。verify 参数三档：`"full"`（默认，foreground+diff）、`"light"`（仅 foreground）、`"none"`（完全跳过）。其他 desk_* 工具同理
4. **工具描述瘦身** — main.py 中 9 个 FunctionTool 的 description 和 parameters.description 压缩至最小必要信息，目标从当前 ~2000 token 降至 ~800 token

## 约束

│  M1 · 所有新参数均为可选，默认行为与当前完全一致
│  M2 · mode="interactive" 的角色白名单写在 scanner.py 常量中，易扩展
│  M3 · mode="coords" 与 target 参数正交，不互斥
│  M4 · verify 参数三档枚举值：full / light / none
│  M5 · 工具描述压缩为每工具 ≤2 行中文，parameters 描述 ≤15 字

## 改动文件

│ 文件                  │ 操作       │ 行数  │
│───────────────────────│────────────│───────│
│ engine/scanner.py     │ 改         │ ~30   │
│ engine/actuator.py    │ 改         │ ~25   │
│ main.py               │ 改         │ ~60   │

## 不改动

- desk_screenshot / desk_window / desk_press 的逻辑 — 验证分级改 actuator 底层，自动覆盖
- desk_state handler 结构 — 透传新参数即可
- _verify 输出格式 — verify="none" 时 _verify 字段为 None 或不存在，由 verifier 决定

─────────────────────────────────────────────────────────
弥亚 · 2026-06-12 13:49
