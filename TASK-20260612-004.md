┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 批复工单                      │
│                                                         │
│  编号: TASK-20260612-004                                │
│  主题: 审计 P1 修复——缓存 id 统一/窗口关闭验证/schema 一致   │
│  指派: opencode                                         │
│  基线: c65ce53                                           │
│  优先级: 中                                              │
│  改动量: 2 文件 verifier.py + actuator.py                │
└─────────────────────────────────────────────────────────┘

## 审计来源

桌面文件 `审计1.txt`，弥亚已批复：全部通过。

## 需求

修复 3 个严重级问题：

1. **缓存不一致：verifier 复用 ControlCache** — verifier.py 轻量扫描目前独立分配 id（hash），应与 scanner 共享同一个 ControlCache 实例，确保 focus_changed 比较的是同一套控件标识体系
2. **窗口关闭后验证目标错误** — window_action("close") 和 ("min") 后窗口可能已销毁，after 信号不应沿用旧 target_rect。close/min 后跳过 pixel diff，仅报 foreground/title 变化；非 close/min 操作保持原逻辑
3. **失败 _verify schema 一致** — 操作异常时 _verify 字典保持与 compare_signals 相同的字段结构（foreground_changed/visual_changed 等），在根级增加 `success: false` 和 `error` 字段，而非替换整个 _verify

## 约束

│  M1 · verifier 获取 cache 实例而非创建新的
│  M2 · close/min 仅收集 foreground/title 信号，不截图不 diff
│  M3 · schema 统一后，LLM 可用同一路径解析成功和失败响应

## 改动文件

│ 文件                  │ 操作       │ 行数  │
│───────────────────────│────────────│───────│
│ engine/verifier.py    │ 改         │ ~25   │
│ engine/actuator.py    │ 改         │ ~20   │

## 不改动

- cache.py 接口 — 无需新增方法，verifier 直接调 get_global_cache()
- 其他工具的 verify 逻辑 — 本单只改 verifier 和 actuator 的核心路径
- pixel diff 算法 — 已在 TASK-003 中修复，不重复

─────────────────────────────────────────────────────────
弥亚 · 2026-06-12 13:49
