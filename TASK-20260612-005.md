┌─────────────────────────────────────────────────────────┐
│              弥亚 DeskHand · 批复工单                      │
│                                                         │
│  编号: TASK-20260612-005                                │
│  主题: 审计 P2 修复——配置缓存/矩形校验/语义清理/RuntimeId/截图释放 │
│  指派: opencode                                         │
│  基线: c65ce53                                           │
│  优先级: 低                                              │
│  改动量: 3 文件 scanner.py + verifier.py + desk_screenshot.py │
└─────────────────────────────────────────────────────────┘

## 审计来源

桌面文件 `审计1.txt`，弥亚已批复：全部通过。

## 需求

修复 5 个轻微级问题：

1. **配置重复读取加缓存** — scanner.py 和 cache.py 中 _get_config() 在模块加载时读一次并缓存，不再每次调用都读文件。提供显式默认配置回退（const 字典）
2. **截图矩形有效性检查** — verifier.py capture_signals 中，target_rect 用前校验 `left < right and top < bottom and area > 0`，无效时填 None 到 errors
3. **_get_focused_state 语义统一** — 三种返回值（None/空dict/含role的dict）统一为 `{"status": "no_focus" | "not_scanned" | "focused", "role": ..., "name": ...}`
4. **RuntimeId 空时继续扫描** — scanner.py 中 runtime_id 为空时不丢弃子树，分配临时 id
5. **desk_screenshot.py img close** — 同审计 #2，加 with 或 close

## 约束

│  M1 · 配置缓存为模块级变量，导入时初始化
│  M2 · RuntimeId 空时临时 id 以负数区分，防止与正常 id 冲突
│  M3 · 与其他工单（TASK-003 #2）的 Image close 改动不冲突

## 改动文件

│ 文件                  │ 操作       │ 行数  │
│───────────────────────│────────────│───────│
│ engine/scanner.py     │ 改         │ ~20   │
│ engine/verifier.py    │ 改         │ ~25   │
│ tools/desk_screenshot.py │ 改      │ ~5    │

## 不改动

- 配置 schema — 不增加新配置项，只优化读取
- compare_signals 主逻辑 — 语义清理仅在 _get_focused_state 内部

─────────────────────────────────────────────────────────
弥亚 · 2026-06-12 13:49
